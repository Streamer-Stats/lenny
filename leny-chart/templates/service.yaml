apiVersion: v1
kind: Service
metadata:
  name: {{ include "leny-chart.fullname" . }}
  labels:
    {{- include "leny-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "leny-chart.selectorLabels" . | nindent 4 }}
