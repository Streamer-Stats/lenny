apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "leny-chart.fullname" . }}-test-connection"
  labels:
    {{- include "leny-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "leny-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
